let myArray = [1, 2, 3, "hello", false]

myArray[0] = "this is a new value"

print("the length of the array is " + len(myArray))

let index = 0
while index < len(myArray)
	print("myArray[" + index + "] = " + myArray[index])
	index = index + 1
end
